# In particular, the container should write the first five terms of the Fibonacci sequence,
# starting from zero and formatted as integer numbers separated by spaces.
# The sequence is generated by this shell script, which obtains the number of terms to print through an environment variable called VAR
# It assumes that the command to be executed is named fibonacci.sh.

# The base image must be alpine:3.12.
FROM alpine:3.12

# It includes a VAR environment variable set to a value 5.
ENV VAR=5

# It specifies /usr/app as the working directory.
WORKDIR /usr/app

# The script must be copied into the container image.
COPY fibonacci.sh ./

RUN ["/bin/sh","fibonacci.sh"]
